---
title: The enterprise agent catalog
description: The enterprise catalog of the Patterson agent platform. Engineering standards and brand identity, encoded as installable agent plugins.
template: splash
hero:
  title: Patterson Corp
  tagline: Patterson's institutional knowledge, encoded as installable agent plugins.
  image:
    alt: Patterson Companies
    dark: ../../assets/patterson-logo-white.svg
    light: ../../assets/patterson-logo-navy.svg
  actions:
    - text: Browse the catalog
      link: /plugins/
      icon: right-arrow
      variant: primary
    - text: Read the architecture
      link: /architecture/
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Trusted Expertise. Unrivaled Support.

This is the enterprise catalog of the Patterson agent platform. It holds the capability that is
true for **all** of Patterson — engineering standards and brand identity — as two independently
installable plugins.

The organizing intent is that an agent working on Patterson code behaves the way a well-oriented
Patterson colleague would: aware of the standards that apply, able to cite them, and able to say
when a standard does not cover the situation at hand.

<ul class="pat-stats">
  <li><span class="pat-stat-figure">2</span><span class="pat-stat-label">plugins, installed independently</span></li>
  <li><span class="pat-stat-figure">12</span><span class="pat-stat-label">skills, every claim traced to a source</span></li>
  <li><span class="pat-stat-figure">9</span><span class="pat-stat-label">capability specifications on record</span></li>
  <li><span class="pat-stat-figure">2</span><span class="pat-stat-label">compliance reviewers you can delegate to</span></li>
</ul>

## Install it

```bash
# inside Claude Code
/plugin marketplace add patterson-agents/patterson-corp
/plugin install patterson-engineering@patterson-corp
/plugin install patterson-brand@patterson-corp
```

Then use it three ways:

```text
"does this pipeline meet our standards?"        ← skill fires automatically
"drop the Patterson theme into this project"    ← design-tokens installs theme.css
"review this page for brand compliance"         ← delegates to the reviewer subagent
```

The same catalog is consumed by VS Code and GitHub Copilot, which read the same
`extraKnownMarketplaces` and `enabledPlugins` keys and defer to the same manifest schema.

<CardGrid stagger>
  <Card title="Standards that cite themselves">
    Patterson's IT standards for delivery, infrastructure, data and operations — each with the
    ServiceNow record it came from, executable validators, and a compliance-reviewer subagent.
    [Read patterson-engineering](/plugins/patterson-engineering/)
  </Card>
  <Card title="Brand identity as tokens">
    The palette and each color's role, Proxima Nova typography, the sentence-case rule, and a
    drop-in Tailwind v4 and shadcn/ui theme generated from tokens.
    [Read patterson-brand](/plugins/patterson-brand/)
  </Card>
  <Card title="Specified before it is built">
    Every substantive change is proposed and specified before any code is written, then folded
    into the capability specification it satisfies.
    [Read the specifications](/specifications/)
  </Card>
  <Card title="Silence is recorded, not filled">
    Where a Patterson source is silent, the gap is written down and escalated rather than
    resolved locally. The platform never manufactures policy.
    [Read the provenance](/provenance/)
  </Card>
</CardGrid>

## What is underneath

`patterson-corp` is the **enterprise** layer of a six-layer model. Lower layers extend it by
default and may override it when they know better — divergence is treated as information about
where a standard is incomplete, not as a violation to suppress.

Nothing in that model is enforced on a developer machine today. The layer configuration
demonstrates the shape of ownership; the enforcement switches are documented and deliberately
left off. [The six-layer model](/architecture/six-layer-model/) explains why, and
[the diagrams](/architecture/diagrams/) show it.

Every page under Plugins, Decisions, Specifications and Governance is generated from a Markdown
file in the repository and links back to it. The repository is the source of truth; this site is
a reading surface for it.
