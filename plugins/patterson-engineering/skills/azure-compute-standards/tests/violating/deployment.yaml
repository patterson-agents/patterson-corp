# Fixture: Kubernetes manifest violating the AKS rules.
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: default
spec:
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  template:
    spec:
      automountServiceAccountToken: true
      containers:
        - name: api
          securityContext:
            privileged: true
            readOnlyRootFilesystem: false
            capabilities:
              add: ["CAP_SYS_ADMIN"]
